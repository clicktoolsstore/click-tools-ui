import { Meta } from "@storybook/addon-docs";
import { BorderRadiosList } from "../../components/BorderRadiosList";

<Meta title={"Tokens/Border Radius"} />

# Border Radius

<BorderRadiosList />
Custom classes marked with * require extending the tailwind
<code>borderRadius</code> configuration.
