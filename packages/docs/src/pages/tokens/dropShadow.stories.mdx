import { Meta } from "@storybook/addon-docs";
import { DropShadowList } from "../../components/DropShadowList";

<Meta title={"Tokens/Drop Shadow"} />

# Drop Shadow

<DropShadowList />
Custom classes marked with * require extending the tailwind config
<code>dropShadow</code> configuration.
