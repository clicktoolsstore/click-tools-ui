import { Meta } from "@storybook/addon-docs";
import { BoxShadowList } from "../../components/BoxShadowList";

<Meta title={"Tokens/Border Shadow"} />

# Box Shadow

<BoxShadowList />
Custom classes marked with * require extending the tailwind config
<code>boxShadow</code> configuration.
